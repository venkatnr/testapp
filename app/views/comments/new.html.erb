Title: <%= @article.title %>  (<%= link_to "back to create new article", new_article_path  %>)<br>
Description: <%= @article.description %>
<%= form_for @comment, :url => {:action => :create, :id => @article.id} do |f| %>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :commentonarticle %><br />
    <%= f.text_area :commentonarticle %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


<%= link_to 'Back', comments_path %>
